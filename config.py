# Some providers supply free API keys
# Scrapingdog: 1000 credits per month, 5 credit per google scholar search
# Serpapi: 250 searches per month, 1 search per google scholar search
# You can supply more than one API key for each provider, and the program will automatically select the key with the most remaining credits for each search.

SCRAP_API_KEY = {
    "scrapingdog": ["using your own key"],
    "serpapi": ["using your own key"],
}